{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && node scripts/patch-blog-hreflang.mjs && node scripts/generate-seo-geo.mjs && vite build && node scripts/generate-i18n-blog-pages.mjs && node scripts/generate-locale-inner-pages.mjs && node scripts/submit-indexnow.mjs && cp en.html de.html ar.html ru.html fr.html dist/ && for d in en de fr ar ru; do [ -d \"$d\" ] && cp -r \"$d\" dist/; done",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "i18next": "^26.0.4"
  }
}
